import { Injectable } from '@nestjs/common';
import { Model } from 'mongoose';

@Injectable()
export class RemindersService {
  constructor(
  ) {}

  async findAllReminders(userId: string) {

  }

  async createReminder(userId: string) {

  }
}
